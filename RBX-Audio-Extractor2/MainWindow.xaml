<Window x:Class="MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="RBX Asset Extractor" Width="1066" Height="647" MinWidth="1066" MinHeight="647"
        Background="{StaticResource CanvasBrush}" Foreground="{StaticResource TextBrush}"
        WindowStyle="None" ResizeMode="CanResize" WindowStartupLocation="CenterScreen"
        Loaded="MainWindow_Loaded" StateChanged="MainWindow_StateChanged" Closing="MainWindow_Closing">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="0"/>
    </shell:WindowChrome.WindowChrome>
    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Background="{StaticResource CanvasBrush}">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="44"/><RowDefinition Height="*"/><RowDefinition Height="34"/></Grid.RowDefinitions>
            <Grid Grid.Row="0" Background="{StaticResource SidebarBrush}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" MouseMove="TitleBar_MouseMove" MouseLeftButtonUp="TitleBar_MouseLeftButtonUp">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="46"/><ColumnDefinition Width="46"/><ColumnDefinition Width="46"/></Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                    <Border Width="25" Height="25" CornerRadius="7" Background="{StaticResource AccentBrush}"><TextBlock Text="R" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                    <TextBlock Text="RBX Asset Extractor" Margin="10,0,0,0" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <Border Margin="10,0,0,0" Height="28" MinWidth="44" Padding="8,0" CornerRadius="5" Background="#2A2538" VerticalAlignment="Center"><TextBlock x:Name="VersionBadgeText" FontSize="10" FontWeight="Bold" Foreground="{StaticResource AccentBrightBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"/></Border>
                </StackPanel>
                <Button x:Name="ScanAllButton" Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,12,0"
                        Style="{StaticResource PrimaryButton}" MinHeight="0" Height="30" Padding="16,0" FontSize="12"
                        Content="Scan all" ToolTip="Scan every category and populate all tabs at once"
                        Click="ScanAllButton_Click" shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
                <Button Grid.Column="1" Style="{StaticResource IconButton}" Click="MinimizeButton_Click" ToolTip="Minimize" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <Path Width="12" Height="12" Data="M 1,7 L 11,7" Stroke="{StaticResource TextBrush}" StrokeThickness="1.4" Stretch="Uniform" SnapsToDevicePixels="True"/>
                </Button>
                <Button Grid.Column="2" Style="{StaticResource IconButton}" Click="MaximizeButton_Click" ToolTip="Maximize or restore" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <Path Width="11" Height="11" Data="M 1,1 L 10,1 L 10,10 L 1,10 Z" Stroke="{StaticResource TextBrush}" StrokeThickness="1.3" Fill="Transparent" Stretch="Uniform" SnapsToDevicePixels="True"/>
                </Button>
                <Button Grid.Column="3" Style="{StaticResource IconButton}" Click="CloseButton_Click" ToolTip="Close" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <Path Width="11" Height="11" Data="M 1,1 L 10,10 M 10,1 L 1,10" Stroke="{StaticResource TextBrush}" StrokeThickness="1.4" Stretch="Uniform" SnapsToDevicePixels="True"/>
                </Button>
            </Grid>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions><ColumnDefinition Width="204"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <Border Background="{StaticResource SidebarBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,1,0">
                    <Grid Margin="12,16,12,12">
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                        <StackPanel>
                            <TextBlock Text="WORKSPACE" Style="{StaticResource EyebrowText}" Margin="12,0,0,9"/>
                            <RadioButton x:Name="OverviewNav" GroupName="Navigation" Content="Overview" Tag="&#xE80F;" Style="{StaticResource NavButton}" IsChecked="True" Checked="Navigation_Checked"/>
                            <RadioButton x:Name="AudioNav" GroupName="Navigation" Content="Audio" Tag="&#xE8D6;" Style="{StaticResource NavButton}" Checked="Navigation_Checked"/>
                            <RadioButton x:Name="VideosNav" GroupName="Navigation" Content="Videos" Tag="&#xE714;" Style="{StaticResource NavButton}" Checked="Navigation_Checked"/>
                            <RadioButton x:Name="ImagesNav" GroupName="Navigation" Content="Images" Tag="&#xEB9F;" Style="{StaticResource NavButton}" Checked="Navigation_Checked"/>
                            <RadioButton x:Name="MeshesNav" GroupName="Navigation" Content="Meshes" Tag="&#xE7C3;" Style="{StaticResource NavButton}" Checked="Navigation_Checked"/>
                            <RadioButton x:Name="ExtrasNav" GroupName="Navigation" Content="More assets" Tag="&#xF133;" Style="{StaticResource NavButton}" Checked="Navigation_Checked"/>
                            <TextBlock Text="SYSTEM" Style="{StaticResource EyebrowText}" Margin="12,22,0,9"/>
                            <RadioButton x:Name="MaintenanceNav" GroupName="Navigation" Content="Maintenance" Tag="&#xE90F;" Style="{StaticResource NavButton}" Checked="Navigation_Checked"/>
                            <RadioButton x:Name="AboutNav" GroupName="Navigation" Content="About &amp; logs" Tag="&#xE946;" Style="{StaticResource NavButton}" Checked="Navigation_Checked"/>
                        </StackPanel>
                        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="13" Background="#1A1B24">
                            <StackPanel><TextBlock Text="LOCAL CACHE" Style="{StaticResource EyebrowText}"/><TextBlock x:Name="SidebarCacheState" Text="Ready to scan" Margin="0,7,0,2" FontWeight="SemiBold"/><TextBlock x:Name="SidebarCachePath" Text="rbx-storage.db" Style="{StaticResource MutedText}" TextTrimming="CharacterEllipsis"/></StackPanel>
                        </Border>
                    </Grid>
                </Border>
                <Grid x:Name="PageSurface" Grid.Column="1" ClipToBounds="True" RenderTransformOrigin="0.5,0.5">
                    <Grid.RenderTransform><TranslateTransform/></Grid.RenderTransform>
                    <Grid.RowDefinitions><RowDefinition Height="78"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <StackPanel Margin="20,14,20,8"><TextBlock x:Name="PageTitleText" Text="Overview" Style="{StaticResource PageTitle}"/><TextBlock x:Name="PageSubtitleText" Text="Explore and export assets directly from Roblox's local cache." Style="{StaticResource MutedText}" Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/></StackPanel>
                    <ContentControl x:Name="PageHost" Grid.Row="1" Margin="20,0,20,16"/>
                </Grid>
            </Grid>
            <Border Grid.Row="2" Background="{StaticResource SidebarBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
                <Grid Margin="14,0"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="180"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBlock x:Name="StatusText" Text="Ready" Style="{StaticResource MutedText}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                    <ProgressBar x:Name="WorkProgress" Grid.Column="1" Minimum="0" Maximum="100" Value="0" VerticalAlignment="Center" Margin="16,0"/>
                    <CheckBox x:Name="AlwaysOnTopCheckBox" Grid.Column="2" Content="Always on top" Checked="AlwaysOnTopCheckBox_Changed" Unchecked="AlwaysOnTopCheckBox_Changed"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
<Window x:Class="FontPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Font Preview" Width="860" Height="700" MinWidth="680" MinHeight="520"
        Background="{StaticResource CanvasBrush}" Foreground="{StaticResource TextBrush}"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="74"/><RowDefinition Height="*"/><RowDefinition Height="34"/></Grid.RowDefinitions>
        <Border Background="{StaticResource SidebarBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="20,0"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center"><TextBlock x:Name="TitleText" FontSize="18" FontWeight="SemiBold"/><TextBlock x:Name="DetailsText" Style="{StaticResource MutedText}" Margin="0,5,0,0"/></StackPanel>
                <Button Grid.Column="1" Content="Close" Style="{StaticResource SecondaryButton}" VerticalAlignment="Center" Click="CloseButton_Click"/>
            </Grid>
        </Border>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="18">
                <Border Style="{StaticResource Card}" Margin="0,0,0,14">
                    <StackPanel><TextBlock Text="CUSTOM SAMPLE" Style="{StaticResource EyebrowText}"/><TextBlock Text="Change this text to inspect the selected font." Style="{StaticResource MutedText}" Margin="0,5,0,12"/><Border Height="40" Style="{StaticResource RoundedTextBoxShell}"><TextBox x:Name="SampleInput" Style="{StaticResource SingleLineTextBox}" Text="The quick brown fox jumps over the lazy dog."/></Border></StackPanel>
                </Border>
                <Border Style="{StaticResource Card}" Margin="0,0,0,14">
                    <StackPanel><TextBlock Text="DISPLAY" Style="{StaticResource EyebrowText}"/><TextBlock x:Name="LargeSample" Text="{Binding Text, ElementName=SampleInput}" FontSize="48" TextWrapping="Wrap" Margin="0,10,0,6"/><TextBlock x:Name="MediumSample" Text="{Binding Text, ElementName=SampleInput}" FontSize="30" TextWrapping="Wrap" Margin="0,4"/><TextBlock x:Name="SmallSample" Text="{Binding Text, ElementName=SampleInput}" FontSize="18" TextWrapping="Wrap" Margin="0,4"/></StackPanel>
                </Border>
                <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <Border Style="{StaticResource Card}" Margin="0,0,7,0"><StackPanel><TextBlock Text="CHARACTERS" Style="{StaticResource EyebrowText}"/><TextBlock x:Name="AlphabetSample" Text="ABCDEFGHIJKLMNOPQRSTUVWXYZ&#x0a;abcdefghijklmnopqrstuvwxyz" FontSize="22" TextWrapping="Wrap" LineHeight="32" Margin="0,11,0,0"/></StackPanel></Border>
                    <Border Grid.Column="1" Style="{StaticResource Card}" Margin="7,0,0,0"><StackPanel><TextBlock Text="NUMBERS &amp; SYMBOLS" Style="{StaticResource EyebrowText}"/><TextBlock x:Name="SymbolSample" Text="0123456789&#x0a;! @ # $ % &amp; * ( ) [ ] { } ?" FontSize="22" TextWrapping="Wrap" LineHeight="32" Margin="0,11,0,0"/></StackPanel></Border>
                </Grid>
            </StackPanel>
        </ScrollViewer>
        <Border Grid.Row="2" Background="{StaticResource SidebarBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0"><TextBlock Text="Private preview only — the font is not installed on Windows." Style="{StaticResource MutedText}" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
    </Grid>
</Window>
<Window x:Class="VideoPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Video Preview" Width="920" Height="680" MinWidth="700" MinHeight="520"
        WindowStyle="None" ResizeMode="CanResize" WindowStartupLocation="CenterOwner"
        Background="{StaticResource CanvasBrush}" Foreground="{StaticResource TextBrush}"
        Loaded="Window_Loaded" Closed="Window_Closed" PreviewKeyDown="Window_PreviewKeyDown">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="44" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Background="{StaticResource CanvasBrush}">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="44"/><RowDefinition Height="82"/><RowDefinition Height="*"/><RowDefinition Height="112"/></Grid.RowDefinitions>
            <Grid Grid.Row="0" Background="{StaticResource SidebarBrush}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="46"/><ColumnDefinition Width="46"/><ColumnDefinition Width="46"/></Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                    <Border Width="25" Height="25" CornerRadius="7" Background="{StaticResource AccentBrush}"><TextBlock Text="▶" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                    <TextBlock x:Name="WindowTitleText" Text="Video preview" Margin="10,0,0,0" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
                <Button Grid.Column="1" Style="{StaticResource IconButton}" Click="MinimizeButton_Click" ToolTip="Minimize" shell:WindowChrome.IsHitTestVisibleInChrome="True"><Path Width="12" Height="12" Data="M 1,7 L 11,7" Stroke="{StaticResource TextBrush}" StrokeThickness="1.4"/></Button>
                <Button Grid.Column="2" Style="{StaticResource IconButton}" Click="MaximizeButton_Click" ToolTip="Maximize or restore" shell:WindowChrome.IsHitTestVisibleInChrome="True"><Path Width="11" Height="11" Data="M 1,1 L 10,1 L 10,10 L 1,10 Z" Stroke="{StaticResource TextBrush}" StrokeThickness="1.3" Fill="Transparent"/></Button>
                <Button Grid.Column="3" Style="{StaticResource IconButton}" Click="CloseButton_Click" ToolTip="Close" shell:WindowChrome.IsHitTestVisibleInChrome="True"><Path Width="11" Height="11" Data="M 1,1 L 10,10 M 10,1 L 1,10" Stroke="{StaticResource TextBrush}" StrokeThickness="1.4"/></Button>
            </Grid>
            <Grid Grid.Row="1" Margin="22,12,22,10">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel><TextBlock x:Name="VideoNameText" FontSize="20" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/><TextBlock x:Name="VideoDetailsText" Style="{StaticResource MutedText}" Margin="0,6,0,0"/></StackPanel>
                <Border Grid.Column="1" Padding="12,7" CornerRadius="7" Background="#1B2C34" BorderBrush="#365767" BorderThickness="1" VerticalAlignment="Center"><TextBlock x:Name="CodecText" Text="VP9 + Opus" Foreground="{StaticResource AccentBrightBrush}" FontSize="11" FontWeight="Bold"/></Border>
            </Grid>
            <Border Grid.Row="2" Margin="22,0" CornerRadius="12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Background="#030508" ClipToBounds="True">
                <Grid>
                    <wv2:WebView2 x:Name="Player" DefaultBackgroundColor="#030508"/>
                    <StackPanel x:Name="PlaybackErrorPanel" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="560" Visibility="Collapsed">
                        <TextBlock Text="Video preview unavailable" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="PlaybackErrorText" Style="{StaticResource MutedText}" TextWrapping="Wrap" TextAlignment="Center" Margin="0,10,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
            <Border Grid.Row="3" Margin="22,12,22,18" Style="{StaticResource Card}" Padding="14">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="190"/></Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center"><Button x:Name="PlayPauseButton" Content="Pause" Style="{StaticResource PrimaryButton}" Width="86" Click="PlayPauseButton_Click"/><Button Content="Stop" Style="{StaticResource SecondaryButton}" Width="74" Margin="8,0,0,0" Click="StopButton_Click"/></StackPanel>
                    <Grid Grid.Column="1" Margin="18,0"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="110"/></Grid.ColumnDefinitions><TextBlock x:Name="SegmentText" Text="Segment 1 of 1" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/><TextBlock x:Name="TimeText" Grid.Column="1" Text="00:00 / 00:00" Style="{StaticResource MutedText}" HorizontalAlignment="Right"/></Grid>
                        <Slider x:Name="PositionSlider" Grid.Row="1" Minimum="0" Maximum="1" Margin="0,10,0,0" PreviewMouseLeftButtonDown="PositionSlider_PreviewMouseLeftButtonDown" PreviewMouseMove="PositionSlider_PreviewMouseMove" PreviewMouseLeftButtonUp="PositionSlider_PreviewMouseLeftButtonUp"/>
                    </Grid>
                    <StackPanel Grid.Column="2" VerticalAlignment="Center"><TextBlock Text="VOLUME" Style="{StaticResource EyebrowText}"/><Slider x:Name="VolumeSlider" Minimum="0" Maximum="1" Value="0.85" Margin="0,10,0,0" ValueChanged="VolumeSlider_ValueChanged"/></StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
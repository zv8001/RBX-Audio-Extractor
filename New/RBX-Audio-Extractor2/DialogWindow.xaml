<Window x:Class="DialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Message" Width="520" MinHeight="220" MaxHeight="650"
        SizeToContent="Height" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner" PreviewKeyDown="DialogWindow_PreviewKeyDown">
    <Border Margin="18" CornerRadius="14" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Border.Effect><DropShadowEffect BlurRadius="24" ShadowDepth="8" Opacity="0.45" Color="#000000"/></Border.Effect>
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="54"/><RowDefinition Height="Auto"/><RowDefinition Height="68"/></Grid.RowDefinitions>
            <Border Background="{StaticResource SidebarBrush}" CornerRadius="14,14,0,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid Margin="16,0,8,0"><Grid.ColumnDefinitions><ColumnDefinition Width="36"/><ColumnDefinition Width="*"/><ColumnDefinition Width="42"/></Grid.ColumnDefinitions>
                    <Border x:Name="IconBadge" Width="28" Height="28" CornerRadius="8" VerticalAlignment="Center" Background="{StaticResource AccentBrush}">
                        <TextBlock x:Name="IconText" Text="i" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock x:Name="TitleText" Grid.Column="1" Margin="10,0,8,0" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                    <Button Grid.Column="2" Style="{StaticResource IconButton}" Click="CloseButton_Click" ToolTip="Close">
                        <Path Width="10" Height="10" Data="M 1,1 L 9,9 M 9,1 L 1,9" Stroke="{StaticResource TextBrush}" StrokeThickness="1.4" Stretch="Uniform"/>
                    </Button>
                </Grid>
            </Border>
            <ScrollViewer Grid.Row="1" MaxHeight="470" VerticalScrollBarVisibility="Auto" Padding="22,24,22,20">
                <TextBlock x:Name="MessageText" FontSize="14" LineHeight="21" TextWrapping="Wrap" Foreground="{StaticResource TextBrush}"/>
            </ScrollViewer>
            <Border Grid.Row="2" Background="#181A22" CornerRadius="0,0,14,14" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
                <StackPanel x:Name="ButtonPanel" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="16,0"/>
            </Border>
        </Grid>
    </Border>
</Window>